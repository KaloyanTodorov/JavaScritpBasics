function printBinaryLogarithm(array) {
    // array.forEach(function (element) {
    //     console.log(Math.log2(element));
    // })

    for(number of array) {
        console.log(Math.log2(number));
    }
}

printBinaryLogarithm([1024,
    1048576,
    256,
    1,
    2,
    50,
    100]);