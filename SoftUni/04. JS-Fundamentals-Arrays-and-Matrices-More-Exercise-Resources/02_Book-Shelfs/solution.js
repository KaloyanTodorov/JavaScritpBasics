function solve() {
  let arr = JSON.parse(document.querySelector('#arr').value);
  let result = document.querySelector('#result');

  let resultContainer = {};
  arr.forEach(element => {
    let patternGenre = /^(\d+ -> \w+)$/gi;
    let patternBookTitle = /as/gi;
    
  });
}